import { useState, useEffect } from 'react';

/**Custom hook to render components based upon viewport size */

const useViewPort = () => {
    /*Declare state variable to hold viewport width*/
    const [width, setWidth] = useState(window.innerWidth);


}